apiVersion: v1
kind: Service
metadata:
  name: devops-project-svc
  labels:
    app: devops-project
spec:
  type: {{ .Values.service.type }}
  selector:
    app: devops-project
  ports:
    - name: http
      protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: 8080