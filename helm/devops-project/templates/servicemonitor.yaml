apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: {{ .Release.Name }}-monitor
  labels:
    release: prometheus
spec:
  selector:
    matchLabels:
      app: devops-project
  endpoints:
    - port: http
      path: /actuator/prometheus
      interval: 15s